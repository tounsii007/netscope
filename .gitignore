# Node
node_modules/
.next/
out/
.env
.env.local
.env*.local

# Java / Maven
api/target/
*.class
*.jar
!maven-wrapper.jar
.mvn/wrapper/maven-wrapper.jar
HELP.md

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Logs â€” daily rotating files (never commit log data)
logs/
*.log
*.log.gz
# Winston audit files (track which log files were created)
.server-audit.json
.error-audit.json
.access-audit.json

# Test artifacts
coverage/
playwright-report/
test-results/
.vitest/
api/target/pit-reports/
api/target/site/
# k6 load-test outputs â€” keep the folder (.gitkeep) but never the runs
web/tests/load/results/*
!web/tests/load/results/.gitkeep

# Secrets
*.pem
*.key
GeoLite2-*.mmdb

# Claude Code agent workspace (worktrees, MCP state, local settings)
.claude/

# Build artefacts
cli/cli.exe
sdk/typescript/dist/
*.tsbuildinfo
